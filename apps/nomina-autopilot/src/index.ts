// nomina-autopilot
//
// Motor declarativo de reglas sobre ingresos. Detecta un depósito entrante
// que supere un umbral configurable (ej. "si entra > USD 1500 en checking")
// y dispara un split previamente definido por el usuario:
// porcentajes a checking, inversión, cripto y pesos. Idempotente por
// transaction id para que un replay no duplique operaciones.

// eslint-disable-next-line no-console
console.log('nomina-autopilot — placeholder');
